# the conda-build parameters to use for disabling --skip-existing
build_parameters:
  - "--suppress-variables"
  #- "--skip-existing"
  - "--error-overlinking"
  - "--error-overdepending"

channels:
  - https://staging.continuum.io/prefect/fs/vs2022-feedstock/pr2/c631724